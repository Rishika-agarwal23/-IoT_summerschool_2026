// Buzzer controlled using TONE

void loop() {
  for (int f = 200; f <= 2000; f += 10) {
    tone(8, f);
    delay(5);
  }
  for (int f = 2000; f >= 200; f -= 10) {
    tone(8, f);
    delay(5);
  }
}
